import { ChangeElemDirective } from "./change-elem.directive";

describe("ChangeElemDirective", () => {
    it("should create an instance", () => {
        const directive = new ChangeElemDirective();
        expect(directive).toBeTruthy();
    });
});
