import { FormatGradePipe } from "./format-grade.pipe";

describe("FormatGradePipe", () => {
    it("create an instance", () => {
        const pipe = new FormatGradePipe();
        expect(pipe).toBeTruthy();
    });
});
